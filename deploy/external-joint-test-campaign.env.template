# Metadata-only file references for the external joint-test evaluation.
# T00 must wire these references into the deployment package/CLI before use.
# Private keys, provider addresses, tokens and raw business messages must not be stored here.
PLATFORM_EXTERNAL_JOINT_TEST_CAMPAIGN_FILE=/etc/chronic-care-platform/external-joint-test-campaign.json
PLATFORM_EXTERNAL_JOINT_TEST_TRUST_REGISTRY_FILE=/etc/chronic-care-platform/external-joint-test-trust-registry.json
PLATFORM_EXTERNAL_JOINT_TEST_EVIDENCE_FILE=/var/lib/chronic-care-platform/evidence/external-joint-test-evidence-bundle.json
