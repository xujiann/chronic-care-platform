# 9 个一级开发域 + 5 个临床子域

> 状态：当前开发组织。该组织定义开发责任，不改变模块化单体、统一主干、共享运行时和生产 `NO-GO`。

## 1. 组织模型

```text
T00 集成治理（不计入九个一级开发域）
  ├─ T01 运行与身份安全
  ├─ T02 平台与状态治理
  ├─ T03 公共卫生
  ├─ T04 居民与慢病
  ├─ T05 服务协同
  ├─ T06 临床专科
  │    ├─ emergency 急救
  │    ├─ blood 血液
  │    ├─ imaging 影像
  │    ├─ physical-examination 体检
  │    └─ quality-safety 质量安全
  ├─ T07 医保支付（独立开发产品线）
  ├─ T08 外部集成
  └─ T09 科研与共享
```

九个一级开发域是 T01–T09。T00 是唯一集成治理单元，负责跨域协议、组合根、路由顺序、CI、治理配置和部署打包，不作为第十个一级开发域。五个临床子域嵌套在 T06 内，因此全平台共有 14 个可规划开发单元，但仍只有九个一级责任域。

## 2. 一级开发域责任

| 域 | 名称 | 主要责任 | 开发边界 |
|---|---|---|---|
| T01 | 运行与身份安全 | 健康、指标、认证授权、会话和安全边界 | 可独立计划、工作树和领域测试 |
| T02 | 平台与状态治理 | 平台治理、审计/发布状态、通用状态适配和医院运行 | 跨域写入与组合变化交 T00 |
| T03 | 公共卫生 | 公卫协同、监测、直报和外部报送 | 外部联调证据不由仓库推断 |
| T04 | 居民与慢病 | 居民档案、家庭授权、慢病和随访 | 居民授权事实由本域提供版本化端口 |
| T05 | 服务协同 | 预约、转诊、护理、陪诊和协同工单 | 领域写入进入 Owner 命令端口 |
| T06 | 临床专科 | 五个临床子域的父级治理和兼容接线 | 新用例归入对应临床子域 |
| T07 | 医保支付 | 医保、支付、退款和凭证 | 金融回调、账本和证据默认失败关闭 |
| T08 | 外部集成 | HIS/EMR/LIS/PACS、网关事件和交换 | 只拥有明确登记的集成边界，不推断数据 Owner |
| T09 | 科研与共享 | 科研沙箱和确实跨域的兼容查询 | `shared` 不得成为业务数据 Owner 或新逻辑收容区 |

名称、路由 Owner 和保护路径唯一以 `config/process-workstreams.json` 为准；本文件不复制 API 清单。

### T07 医保支付产品线

`insurance-payment` 是当前唯一显式确立的独立开发产品线，由 T07 负责产品 Roadmap、Backlog、process 工作树和领域测试。其业务范围继续由 T07 process Owner、现有 API/数据 Owner、按病种付费、金融网关、在线退款、凭证和验收实现共同定义，本组织合同不复制功能、路由或集合清单。

产品线发布仍通过 T00 合入 `main`，运行于共享 Node.js 和模块化单体；`independentDeploymentAuthorized=false`、`productionReady=false`。产品线身份不授权独立仓库、数据库、服务、容器或生产部署。

## 3. 临床五子域责任

| 子域 | Owner | 责任边界 |
|---|---|---|
| 急救 | `T06/emergency` | 急救事件、信号和生命链用例 |
| 血液 | `T06/blood` | 用血、血液库存、输血和血液安全用例 |
| 影像 | `T06/imaging` | 影像云、互认、分享和质控用例 |
| 体检 | `T06/physical-examination` | 体检、专项分流、异常处置和居民解释用例 |
| 质量安全 | `T06/quality-safety` | 质量自有写模型及跨子域质量观察读模型 |

子域、API 前缀、目标源码根、候选集合和跨子域契约唯一以 `config/clinical-subdomains.json` 为准。候选集合不是生产数据 Owner；生产数据权威仍是 `config/domain-data-ownership.json`。

## 4. 开发权与集成权

每个一级域和临床子域可以独立完成：

- 制定领域 PLAN 和维护领域 backlog；
- 使用符合 Owner 的独立 process 工作树；
- 修改自身源码、测试和同步文档；
- 在稳定公共接口内独立评审和交付；
- 建立领域特征、单元、集成和负向测试。

以下事项必须由 T00 集成或 handoff：

- `server.js`、组合根、全局路由顺序和跨域协议；
- process/data Owner、CI、发布和部署配置；
- 数据库 migration、共享平台端口和服务提取决策；
- 跨一级域或跨临床子域的兼容窗口与回滚。

## 5. 统一边界

当前组织固定使用：

- 一个 Git 仓库；
- `main` 唯一集成主干；
- 独立 process 工作树；
- 领域测试加全平台共享门禁；
- 一个共享 Node.js 运行时；
- 模块化单体统一部署。

“可独立开发”不表示可独立部署。任何独立服务仍须满足 `config/service-extraction-scorecard.json` 的评分、数据自治、团队、SLO、运维和现场证据门槛，并另立 ADR。

## 6. 机器门禁

`config/development-organization.json` 只保存 9+5 组合关系、产品线身份和开发政策，引用而不替代现有 Owner 权威。运行：

```powershell
npm.cmd run development-organization:verify
```

门禁拒绝：把 T00 计入一级域、漏掉 T01–T09、改变五子域闭集、漂移 T06 Owner 命名空间、漂移医保支付产品线的 T07 归属或自主开发合同、拆多仓、授权独立部署/生产就绪，或与现有 Owner 清单不一致。
