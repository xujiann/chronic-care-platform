# chronic-care-platform environment template

# HTTP port used by server.js. Defaults to 5173.
PORT=5173

# Runtime environment label shown by /api/health and /api/metrics.
NODE_ENV=production

# Region selection defaults to the generic template. Set an enabled six-digit
# administrative division code only for a region-specific deployment.
REGION_CODE=template
# Pin the registry class in region deployments to prevent a test fixture or
# template from being substituted for a reviewed production region.
REGION_DEPLOYMENT_CLASS=template
# After packaging, set this to the contentDigest emitted by regional:status.
# The runtime will fail closed if any regional manifest, config or extension changes.
# REGION_CONTENT_DIGEST=sha256:replace-with-regional-content-digest
# Central operations probes use server-owned HTTPS roots. Define one variable
# per enabled site; never accept a probe URL from an HTTP request.
# REGIONAL_SITE_000000_BASE_URL=https://health.example.gov.cn
# REGIONAL_SITE_000000_BACKUP_CHECKED_AT=2026-08-06T00:00:00.000Z

# Storage engine: auto, sqlite, or json.
# auto uses node:sqlite when the runtime supports it and keeps data/db.json as a static snapshot.
STORAGE_ENGINE=auto

# Required production SQLite durability profile. The runtime applies these settings to every connection.
SQLITE_JOURNAL_MODE=WAL
SQLITE_SYNCHRONOUS=FULL
SQLITE_BUSY_TIMEOUT_MS=5000
SQLITE_WAL_AUTOCHECKPOINT_PAGES=1000
SQLITE_HEALTH_CHECK_TTL_MS=30000

# Required when STORAGE_ENGINE=postgres/postgresql or SESSION_STORE=postgres.
# DATABASE_URL=postgres://health:replace-with-password@postgres.internal:5432/chronic_care
# PostgreSQL shadow synchronization remains disabled until the migration baseline is loaded and approved.
# POSTGRES_SYNC_MODE=disabled
# POSTGRES_SSL_MODE=verify-full
# POSTGRES_CA_FILE=/run/secrets/postgres-ca.pem
# POSTGRES_POOL_MAX=2
# POSTGRES_SYNC_BACKLOG_SLO_MAX=20
# POSTGRES_SYNC_PENDING_AGE_SLO_SECONDS=300
# POSTGRES_RECONCILIATION_AGE_SLO_SECONDS=600
# POSTGRES_RECONCILIATION_OPEN_CASES_SLO_MAX=0
# Verified primary-read rehearsal remains disabled unless shadow reconciliation is current and approved.
# POSTGRES_PRIMARY_READ_MODE=disabled
# POSTGRES_PRIMARY_READ_MAX_COLLECTIONS=2000
# POSTGRES_PRIMARY_READ_MAX_BYTES=134217728
# PostgreSQL production adapter stays in rehearsal mode until external evidence is recorded.
# POSTGRES_ADAPTER_MODE=disabled
# POSTGRES_PRODUCTION_WRITE_MODE=disabled
# The primary storage contract remains disabled by default. shadow may consume
# committed SQLite outbox batches; primary-read/primary-write remain controlled
# rehearsals and never authorize request-path dual writes or production cutover.
# POSTGRES_PRIMARY_STORAGE_MODE=disabled
# POSTGRES_PRIMARY_POOL_MAX=4
# POSTGRES_PRIMARY_CONNECT_TIMEOUT_MS=5000
# POSTGRES_PRIMARY_IDLE_TIMEOUT_MS=30000
# POSTGRES_PRIMARY_APPLICATION_NAME=health-platform-primary-storage
# POSTGRES_SCHEMA_EVIDENCE_ID=
# POSTGRES_MIGRATION_EVIDENCE_ID=
# POSTGRES_RECONCILIATION_EVIDENCE_ID=
# POSTGRES_ROLLBACK_EVIDENCE_ID=
# POSTGRES_CUTOVER_APPROVAL_ID=
# POSTGRES_BACKUP_EVIDENCE_ID=
# POSTGRES_RTO_RPO_EVIDENCE_ID=

# Optional external data directory. Defaults to ./data.
# DATA_DIR=/var/lib/chronic-care-platform

# Comma-separated session signing secrets. Put the newest first during rotation.
SESSION_SECRETS=replace-with-long-random-secret
# Single-host production uses sqlite. Multi-host production requires postgres plus DATABASE_URL and verify-full TLS.
SESSION_STORE=sqlite
SESSION_TOPOLOGY=single-host
# Retain expired sessions for 7 days and revoked audit rows for 30 days; cleanup runs every 15 minutes.
SESSION_EXPIRED_RETENTION_DAYS=7
SESSION_REVOKED_RETENTION_DAYS=30
SESSION_CLEANUP_INTERVAL_MS=900000

# HMAC signing secret for integration gateway simulation.
INTEGRATION_GATEWAY_SECRET=replace-with-integration-secret

# Production government identity and resident SMS adapters. Required by env:check:production.
# OIDC_ISSUER_URL=https://identity.example.gov.cn/real-issuer
# OIDC_USERINFO_URL=https://identity.example.gov.cn/real-issuer/userinfo
# OIDC_TOKEN_URL=https://identity.example.gov.cn/real-issuer/token
# OIDC_REVOCATION_URL=https://identity.example.gov.cn/real-issuer/revoke
# OIDC_CLIENT_ID=replace-with-oidc-client-id
# OIDC_CLIENT_SECRET=replace-with-oidc-client-secret
# IDENTITY_DIRECTORY_URL=https://identity.example.gov.cn/scim/v2/Users
# IDENTITY_DIRECTORY_TOKEN=replace-with-directory-read-token
# IDENTITY_ADAPTER_TIMEOUT_MS=8000
# CA_GATEWAY_URL=https://ca.example.gov.cn/real-gateway
# SMS_GATEWAY_URL=https://sms.example.gov.cn/real-gateway
# SMS_GATEWAY_TOKEN=replace-with-provider-token
# SMS_TEMPLATE_ID=resident-login-code
# SMS_SENDER=health-platform
# SMS_GATEWAY_TIMEOUT_MS=8000
# SMS_DELIVERY_CALLBACK_SECRET=replace-with-provider-callback-signing-secret
# SMS_DELIVERY_CALLBACK_MAX_SKEW_SECONDS=300

# Hospital core-system outbound adapters. A shared signing secret can be replaced by per-domain secrets.
# HOSPITAL_ADAPTER_SECRET=replace-with-hospital-connector-signing-secret
# HOSPITAL_ADAPTER_TOKEN=replace-with-optional-shared-bearer-token
# HIS_ADAPTER_URL=https://his.example.gov.cn/platform/events
# EMR_ADAPTER_URL=https://emr.example.gov.cn/platform/events
# LIS_ADAPTER_URL=https://lis.example.gov.cn/platform/events
# PACS_ADAPTER_URL=https://pacs.example.gov.cn/platform/events
# APPOINTMENT_ADAPTER_URL=https://his.example.gov.cn/platform/appointments
# HIS_ADAPTER_SECRET=replace-with-optional-his-specific-secret
# EMR_ADAPTER_SECRET=replace-with-optional-emr-specific-secret
# LIS_ADAPTER_SECRET=replace-with-optional-lis-specific-secret
# PACS_ADAPTER_SECRET=replace-with-optional-pacs-specific-secret
# APPOINTMENT_ADAPTER_SECRET=replace-with-optional-appointment-specific-secret
# HOSPITAL_ADAPTER_TIMEOUT_MS=8000
# HOSPITAL_ADAPTER_MAX_ATTEMPTS=3

# Secure attachment object-storage gateway.
# OBJECT_STORAGE_GATEWAY_URL=https://storage.example.gov.cn/api/
# OBJECT_STORAGE_BUCKET=health-platform-attachments
# OBJECT_STORAGE_SIGNING_SECRET=replace-with-object-storage-signing-secret
# OBJECT_STORAGE_TOKEN=replace-with-optional-object-storage-token
# OBJECT_STORAGE_TIMEOUT_MS=8000
# OBJECT_STORAGE_MAX_BYTES=104857600
# OBJECT_STORAGE_DOWNLOAD_TTL_SECONDS=300

# Payment, medical-insurance and electronic-certificate production gateways.
# FINANCIAL_GATEWAY_SECRET=replace-with-shared-financial-signing-secret
# FINANCIAL_GATEWAY_TOKEN=replace-with-optional-shared-bearer-token
# FINANCIAL_CALLBACK_SECRET=replace-with-shared-financial-callback-secret
# FINANCIAL_CALLBACK_MAX_SKEW_SECONDS=300
# PAYMENT_GATEWAY_URL=https://payment.example.gov.cn/platform/transactions
# INSURANCE_GATEWAY_URL=https://insurance.example.gov.cn/platform/settlements
# CERTIFICATE_GATEWAY_URL=https://certificate.example.gov.cn/platform/certificates
# DISEASE_PAYMENT_TRUSTED_SIGNER_FINGERPRINTS=replace-with-comma-separated-sha256-public-key-fingerprints
# PAYMENT_GATEWAY_SECRET=replace-with-optional-payment-specific-secret
# INSURANCE_GATEWAY_SECRET=replace-with-optional-insurance-specific-secret
# CERTIFICATE_GATEWAY_SECRET=replace-with-optional-certificate-specific-secret
# PAYMENT_CALLBACK_SECRET=replace-with-optional-payment-callback-secret
# INSURANCE_CALLBACK_SECRET=replace-with-optional-insurance-callback-secret
# CERTIFICATE_CALLBACK_SECRET=replace-with-optional-certificate-callback-secret
# PAYMENT_GATEWAY_TOKEN=replace-with-optional-payment-token
# INSURANCE_GATEWAY_TOKEN=replace-with-optional-insurance-token
# CERTIFICATE_GATEWAY_TOKEN=replace-with-optional-certificate-token
# FINANCIAL_GATEWAY_TIMEOUT_MS=8000
# FINANCIAL_GATEWAY_MAX_ATTEMPTS=3

# Production audit retention target. At least one of AUDIT_EXPORT_PATH or SIEM_ENDPOINT is required.
# AUDIT_EXPORT_PATH=/var/log/chronic-care-platform/audit
# SIEM_ENDPOINT=https://siem.example.gov.cn/ingest
# SIEM_SIGNING_SECRET=replace-with-siem-alert-signing-secret
# SIEM_TOKEN=replace-with-optional-siem-token
# ALERT_WEBHOOK_URL=https://alerts.example.gov.cn/platform/events
# ALERT_WEBHOOK_SECRET=replace-with-alert-webhook-signing-secret
# ALERT_WEBHOOK_TOKEN=replace-with-optional-alert-webhook-token
# ALERTING_TIMEOUT_MS=8000
# ALERTING_MAX_ATTEMPTS=3
# RETENTION_POLICY=10y-worm

# T11 production security release evidence. This server-side directory must be
# outside the repository and readable only by the release service account.
# HTTP requests cannot override it, and validation never authorizes cutover by itself.
# PRODUCTION_RELEASE_EVIDENCE_DIR=/etc/health-platform/production-release-evidence

# Public-health external resilience policy. Production requires all eight lanes; clients cannot override it.
# PUBLIC_HEALTH_EXTERNAL_RESILIENCE_POLICIES={"infectious-reporting":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100},"immunization":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100},"maternal-child":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100},"senior-health":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100},"chronic-management":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100},"public-health-followup":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100},"health-education":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100},"family-doctor":{"failureThreshold":3,"openSeconds":120,"halfOpenMaxProbes":1,"rateLimitPerMinute":30,"maxPending":100}}

# Public-health external contract governance. Production requires the independent managed keyring reference.
# PUBLIC_HEALTH_EXTERNAL_CONTRACT_GOVERNANCE_KEYRING_REF=vault://health-platform/public-health/contracts/governance
# Retain historical verification keys through every signed transition sunset; never physically delete keys still needed for chain reconstruction.
# PUBLIC_HEALTH_EXTERNAL_CONTRACT_GOVERNANCE_SECRET=local-compatibility-only-at-least-32-characters
# PUBLIC_HEALTH_EXTERNAL_CONTRACT_RELEASE_EVIDENCE=[{"id":"release-evidence-id","status":"deployed-and-verified","laneId":"immunization","fromContract":"immunization-registry-v1","toContract":"immunization-registry-v2","requestSchemaVersion":"public-health-external-dispatch/v2","receiptSchemaVersion":"public-health-external-receipt/v2","changeType":"additive","fieldDictionaryDigest":"64-hex","sampleRequestDigest":"64-hex","sampleReceiptDigest":"64-hex","t08ReleaseDigest":"64-hex","t00ReleaseDigest":"64-hex","runtimeReleaseDigest":"sha256-of-bound-t08-and-t00-digests","producerApproval":{"organizationId":"producer-org","role":"producer-contract-owner","approverIdHash":"64-hex","approvedAt":"ISO-8601"},"consumerApproval":{"organizationId":"consumer-org","role":"consumer-contract-owner","approverIdHash":"64-hex","approvedAt":"ISO-8601"},"evidenceRefs":["field-dictionary-ref"],"jointTestEvidenceRef":"joint-test-ref","rollbackEvidenceRef":"rollback-ref","deployedAt":"ISO-8601","verifiedAt":"ISO-8601"}]
# PUBLIC_HEALTH_RESPIRATORY_NETWORK_EVIDENCE_KEYRING_JSON={"purpose":"public-health-respiratory-network-evidence","activeKeyId":"respiratory-network-evidence-current","keys":[{"keyId":"respiratory-network-evidence-current","secret":"<resolve-from-managed-key-service>","status":"active","notBefore":"ISO-8601","expiresAt":"ISO-8601","revokedAt":""}]}
# PUBLIC_HEALTH_RESPIRATORY_NETWORK_EVIDENCE_SIGNER_ID=managed-respiratory-network-evidence-service

# Production deployment package provenance and secret injection. Values are provided by CI and infrastructure.
# DEPLOYMENT_SECRET_PROVIDER=vault
# DEPLOYMENT_RELEASE_ID=approved-release-id
# DEPLOYMENT_ARTIFACT_DIGEST=sha256:replace-with-64-hex-artifact-digest
# DEPLOYMENT_APP_DIR=/opt/chronic-care-platform/releases/approved-release-id
# DEPLOYMENT_SECRET_ENV_FILE=/run/secrets/chronic-care-platform.env
# DEPLOYMENT_DATA_DIR=/var/lib/chronic-care-platform
# DEPLOYMENT_LOG_DIR=/var/log/chronic-care-platform

# Production cutover site signoff flags. Use signed/approved/true only after the real evidence is archived.
# CUTOVER_SITE_INTERFACE_SIGNOFF=signed
# CUTOVER_CHRONIC_LAUNCH_CORE_SIGNOFF=signed
# CUTOVER_INSURANCE_CERTIFICATE_SIGNOFF=signed
# CUTOVER_MONITORING_SIGNOFF=signed
# CUTOVER_DR_REHEARSAL_SIGNOFF=signed

# Internet nursing and escort transactional runtime. Keep disabled until every endpoint,
# managed secret, monitor and the five signed cutover evidence packets are real.
# CARE_OUTBOX_WORKER_ENABLED=false
# CARE_OUTBOX_WORKER_ID=stable-production-worker-id
# CARE_SERVICE_RUNTIME_MODULE=care-service-production-runtime.js
# CARE_NURSING_DELIVERY_URL=https://nursing-gateway.example.gov.cn/care/events
# CARE_ESCORT_DELIVERY_URL=https://escort-gateway.example.gov.cn/care/events
# CARE_OUTBOX_DELIVERY_SECRET=replace-with-managed-secret-at-least-32-characters
# CARE_OUTBOX_DELIVERY_TIMEOUT_MS=8000
# CARE_OUTBOX_BATCH_SIZE=20
# CARE_OUTBOX_LEASE_SECONDS=60
# CARE_OUTBOX_MAX_ATTEMPTS=5
# CARE_OUTBOX_RETRY_BASE_SECONDS=30
# CARE_OUTBOX_MAX_RETRY_SECONDS=1800
# CARE_OUTBOX_MAX_PENDING_AGE_SECONDS=300
# CARE_CUTOVER_EVIDENCE_FILE=/etc/health-platform/care-service-cutover-evidence.json
# CARE_CUTOVER_EVIDENCE_SHA256=replace-with-sha256-of-exact-evidence-file
# CARE_CUTOVER_SIGNOFF_MAX_AGE_DAYS=30
# CARE_DEPENDENCY_EVIDENCE_FILE=/etc/health-platform/care-service-dependency-evidence.json
# CARE_DEPENDENCY_EVIDENCE_SHA256=replace-with-sha256-of-exact-evidence-file
# CARE_DEPENDENCY_PROBE_MAX_AGE_MINUTES=15
