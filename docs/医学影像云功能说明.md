# 医学影像云功能说明

本模块依据《辽宁省医学影像云功能规范》和《辽宁省医学影像云技术规范》建设区域医学影像云演示能力，承接医院 PACS/RIS/EMR 影像数据入云、患者手机调阅、授权分享、质控统计和电子病历兼容索引。

## 主要功能

- 医院接入：支持前置网关和无前置采集软件两种模式，记录 DICOM C-STORE/C-MOVE、DICOM TLS、RIS 结构化检查信息、审核报告和对象存储地址。
- 影像云存储：按检查保存诊断级 DICOM 序列和浏览级预览索引，记录序列数、图像数、完整性校验、无损压缩和云端对象路径。
- 患者手机查看：`imaging-cloud.html` 提供移动端影像查看模拟，患者实名登录后查看报告、浏览级序列、诊断级状态和限时分享状态，终端不保存原始 DICOM。
- 授权分享：`POST /api/imaging-cloud/studies/:id/share` 生成二维码或短信链接型分享令牌，可设置有效期并写入访问留痕。
- 质控管理：`POST /api/imaging-cloud/studies/:id/qc` 记录扫描评价、报告评分、质控结论和复核意见。
- 电子病历兼容：医院入库接口会同步生成 `diagnosticReports` 和 `personalRecords` 影像记录，EMR 可通过“医疗机构编码 + 身份证号 + 检查号”主索引授权调阅。

## 当前已实现功能

- 已实现医院影像数据接入：`POST /api/imaging-cloud/ingest` 支持接入 DICOM/RIS/PACS 检查，并同步生成影像云检查、诊断报告和居民影像档案。
- 已实现患者手机查看：`imaging-cloud.html` 提供手机影像查看、序列预览、报告结论、分享状态和终端不保存原始 DICOM 的提示。
- 已实现电子病历兼容索引：影像入云后写入 `diagnosticReports` 与 `personalRecords`，并保留 `mainIndex`、`studyInstanceUID`、`imageCloudStudyId` 等调阅字段。
- 已实现授权分享与质控回写：支持影像检查限时分享、二维码/短信链接渠道、质控评分和质控结论回写。
- 已实现跨机构影像互认闭环：影像云检查可生成县域协同工单、互认记录、报告引用和主索引；医共体或卫健端确认后同步检查状态、报告状态与审计证据。
- 已实现不予互认申诉复核：来源机构以最小化证据引用提交申诉，原判定人和申诉提交人不得复核；独立复核结论同步影像、诊断报告、协同工单、引用哈希和审计记录。
- 已实现角色权限与越权保护：监管端、医疗机构、县域平台、居民端按角色访问；居民只能查看本人或家庭授权范围内影像。
- 已实现自动化证据：`imaging-cloud:readiness`、`test/imaging-cloud-readiness.test.js`、`test/static.test.js`、`test/api.test.js` 覆盖页面、API、PWA、EMR 索引同步和越权拒绝。
- 已实现诊断级浏览性能验收模型：`imaging-cloud-planning.js` 对 DICOMweb/WADO-RS、无损诊断源、首像素、序列加载、交互 P95、失败对象率、二维/三维工具、访问审计和终端不落原始 DICOM 设置硬阻断；真实现场运行仍须使用授权端点和摘要证据。
- 已实现监管统计聚合：按日、月、年汇总机构和地市上传、影像对象、诊断、调阅、分享、质控、会诊/互认和数据质量问题，生成排名与异常机构清单，输出中不包含患者、检查号或 StudyInstanceUID。

## 下一步开发计划

| 优先级 | 阶段 | 开发目标 | 下一动作 | 验收口径 |
|---|---|---|---|---|
| P0 | 试点医院联调 | 代码闭环已实现，真实 PACS/RIS/EMR 联调待完成 | 补齐真实联调单号、DICOM TLS 证书、前置网关心跳截图和回传原院 PACS 验收记录 | 至少 1 家医院完成入云、报告回写、居民调阅、EMR 调阅和越权拒绝测试 |
| P0 | 安全与合规加固 | 代码门禁、结构化回执和审计链已实现，现场合规证据待完成 | 补充生产证书、密钥轮换、日志留存、脱敏导出和审计抽查证据 | 所有影像调阅、分享、质控和回传操作可追踪到账号、居民、主索引和用途 |
| P1 | 诊断级浏览能力 | 性能验收模型已实现，真实 DICOMweb 运行待完成 | 接入真实影像浏览器组件或 DICOMweb/WADO-RS 代理并执行 `evaluateViewerPerformance` | 授权诊断源、性能阈值、工具、访问审计和终端存储边界全部通过 |
| P1 | 统计监管大屏 | 统计聚合、排名和异常定位已实现，生产事件流待接入 | 将上传、调阅、分享、质控、会诊和数据质量事件接入 `buildRegulatoryStatistics` | 监管端可按日、月、年查看机构和地市指标并定位异常机构 |
| P2 | 跨机构互认扩展 | 已将影像云纳入县域检查互认演示闭环，后续扩展远程诊断和远程会诊联调 | 已建立与 `countyCollaborationOrders`、`countyMutualRecognitionRecords`、`diagnosticReports` 的引用；下一步接入真实 PACS/RIS 和会诊系统 | 基层申请、中心诊断、报告回传、结果互认和居民查询串联到同一主索引 |

## 接口

- `GET /api/imaging-cloud`：返回影像云汇总、网关、检查、分享、质控、EMR 兼容和安全基线。
- `POST /api/imaging-cloud/ingest`：医院或监管端提交影像检查，生成影像云检查、诊断报告和居民健康信息库影像索引。
- `POST /api/imaging-cloud/studies/:id/share`：患者或授权机构生成限时分享。
- `POST /api/imaging-cloud/studies/:id/qc`：机构或监管端回写质控结果。
- `POST /api/imaging-cloud/studies/:id/mutual-recognition`：机构、医共体或卫健端将影像检查纳入跨机构互认，生成协同工单、互认记录和报告引用。
- `POST /api/imaging-cloud/studies/:id/mutual-recognition/decision`：医共体或卫健端确认或拒绝互认，同步更新影像检查、诊断报告、协同工单和审计证据。
- `POST /api/imaging-cloud/studies/:id/mutual-recognition/appeal`：来源机构对不予互认结果提交理由和最小化证据引用，并确认不包含患者可识别信息。
- `POST /api/imaging-cloud/studies/:id/mutual-recognition/appeal/review`：医共体或卫健端由独立复核人通过或驳回申诉，复用互认引用证据链。

## 安全与部署约束

- 计算与存储资源按规范要求落在本地省域可控环境。
- 传输使用 HTTPS、DICOM TLS、专线或 SD-WAN + IPSEC，敏感结构化数据按 AES 加密要求设计。
- 按等保三级基线组织边界防护、主机防护、运维审计、访问控制和日志留痕。
- 患者影像具有属主性，所有调阅、分享和医生访问都必须经过身份认证、授权和审计。

## 单院合成数据闭环与正式生产门禁

`imaging-cloud-production.js` 将影像云生产状态与功能演示状态分离。初始功能状态为 `ready-for-synthetic-acceptance`，正式上线状态固定为 `blocked-until-site-evidence-signed`；只有合成数据验收、生产端点、现场证据、故障演练和双人审批全部完成后，才允许转为 `ready-for-production`。

### 合成数据十步验收

1. HAPI FHIR、Orthanc、OHIF 三项服务健康。
2. 合成 FHIR Patient 写入与回读。
3. 合成 DICOM 进入 Orthanc。
4. DICOMweb rendered 端点返回可渲染像素。
5. 按 StudyInstanceUID 查询 DICOMweb 检查。
6. 主索引关联具备人工复核证据。
7. Patient、ImagingStudy、DiagnosticReport 引用闭环。
8. OHIF 授权调阅链接可加载。
9. 调阅行为写入访问审计。
10. 无权限居民调阅被拒绝并留痕。

每一步必须声明 `dataClass=synthetic-test-data`、使用精确确认语句并提供证据引用。真实患者数据不得写入合成验收脚本或用于解除合成验收门禁。

### 生产端点与现场要求

生产端点包括 HAPI FHIR、Orthanc、OHIF、试点医院 PACS/RIS 网关、区域主索引与审计服务。探测地址必须使用 HTTPS 或 DICOM TLS，并登记凭据引用和证书 SHA-256 指纹；本机 HTTP 健康检查不能计入生产端点就绪。

七项现场阻断覆盖 PACS/RIS/DICOM TLS 联调、FHIR与EMR回写、主索引和机构人员、实名调阅与审计、等保/密码/隐私、性能灾备及多方验收。机构责任人提交证据时必须登记外部签署人、外部机构、证据引用和 SHA-256 摘要；卫生行政责任人使用相同摘要独立核验，提交人不得核验本人材料。

### 结构化现场回执契约

除通用现场签署外，以下五类回执必须按字段校验并由不同人员完成提交与核验。回执只保存版本、技术状态、外部签署信息和证据摘要；禁止提交患者姓名、身份证号、联系方式、检查号、StudyInstanceUID、原始 DICOM 或影像二进制数据。

| 回执类型 | 覆盖事项 | 必填技术结论 | 现场验收边界 |
| --- | --- | --- | --- |
| `pacs-ris-dicom-tls` | PACS/RIS、影像网关 | `DICOM-TLS`、TLS 1.2+、`C-STORE`、`C-MOVE`、网关回执引用 | 仅登记协议和回执引用，不登记真实检查标识 |
| `fhir-report-writeback` | FHIR/EMR 报告回写 | FHIR R4、`Patient`/`ImagingStudy`/`DiagnosticReport`、回写已接收 | 以系统回执编号证明引用链，不复制病历内容 |
| `object-storage-authorization-audit` | 对象存储、授权调阅、审计 | 省内存储、静态加密、认证授权、审计留存不少于 180 天、移动端不落原始 DICOM | 对象地址、密钥和患者数据不进入回执 |
| `mutual-recognition-appeal` | 互认不通过申诉与复核 | 申诉政策版本、县域/卫健委独立复核、最小化证据声明 | 原判定人和申诉提交人不得复核 |
| `failure-degradation-rollback` | PACS/RIS、FHIR、对象存储故障 | 三类故障场景、原院 PACS 与报告优先降级、对账与回退决策回执 | 演练回执不替代真实灾备签字 |

`runStandaloneSmoke()` 用于模块独立运行的代码级冒烟：验证模块不依赖急救、用血或体检领域，检查结构化回执、防泄露边界、回退触发条件与 No-Go 状态。它只输出 `go/no-go` 判断，现场证据未完成时必须返回 `no-go`。

### T00公共路由接入契约

- `GET /api/imaging-cloud/production-center`
- `POST /api/imaging-cloud/production/endpoints/:id/probe`
- `POST /api/imaging-cloud/production/synthetic-checks/:id/actions`
- `POST /api/imaging-cloud/production/requirements/:id/actions`
- `POST /api/imaging-cloud/production/receipts/:type/submit`
- `POST /api/imaging-cloud/production/receipts/:type/verify`
- `POST /api/imaging-cloud/production/drills/:id/complete`
- `POST /api/imaging-cloud/production/approvals/:id/sign`
- `GET /api/imaging-cloud/production/smoke`

页面“生产操作工作台”覆盖端点探测、合成数据验收、现场证据提交/独立核验/驳回、生产故障演练和双人上线审批。静态预览不会模拟或写入生产证据；只有T00按 `ROUTE_CONTRACTS` 接入公共服务端后表单才会提交。每个契约均声明允许角色和领域处理器，T00只负责鉴权、持久化事务和HTTP错误映射，不得绕过领域门禁直接修改状态。

生产操作审计采用链式摘要：每条审计记录包含上一条 `previousDigest`，第一条从 `GENESIS` 起链，并以当前记录内容生成 `sha256` 摘要。T00 持久化时必须完整保存 `imagingProductionAudit`，不得只保留最新状态字段。

上述领域动作已由 `imaging-cloud-production.js` 实现。公共 `server.js`、`package.json`、Service Worker、README和发布总表由T00统一接入；在公共路由完成前，影像工作台显示只读门禁状态和现场阻断项，不提供伪写入。

### 首增量验收口径

- `test/imaging-cloud-production.test.js` 验证初始阻断、合成数据隔离、不安全端点拒绝、五类结构化回执、患者标识拒绝、外部证据来源、独立核验、端点关联、四项演练和不同人员双审批。
- `scripts/imaging-cloud-readiness.js` 同时输出 `codeReady`、`functionalState`、`formalGoLiveState` 和 `productionReady`，静态检查通过不得自动把 `productionReady` 置为真。
- 独立模块冒烟必须输出 `releaseDecision=no-go`，直至现场证据、结构化回执、演练和双人审批全部通过；生产回滚触发条件至少包括主索引错误关联、越权调阅、影像对象不可恢复、PACS持续回传失败和FHIR引用链完整性失败。
