# 数智医院标准平台开发报告与下一步计划

- 报告日期：2026-07-11
- 开发分支：`codex/merge-chronic-followup-into-platform`
- 当前阶段：MVP 业务功能与生产适配器基础完成，现场生产割接未完成
- 结论：仓库已形成可运行、可测试、可审计、可发布验证的生产前版本；在真实凭据、外部接口联合测试、安全测评、监控灾备和正式签字完成前，不得标记为正式生产就绪。

## 一、本轮主要开发成果

### 1. 生产运行底座

- SQLite 生产配置支持 WAL、FULL 同步、外键、忙等待、自动检查点和 `quick_check` 健康证据；不支持的 PostgreSQL 配置会明确阻断，避免静默降级。
- OIDC UserInfo、受控本地账号绑定、短信随机验证码、摘要存储和供应商受理回执已接入运行时。
- HIS、EMR、LIS、PACS、号源连接器支持 HTTP 出站、HMAC-SHA256、幂等键、有限重试、受理回执、死信和人工对账。
- 安全对象存储支持上传意图、大小与 SHA-256 复核、服务端恶意文件扫描、短时下载授权、隔离、法律保全和不可变留存控制。

### 2. 支付、医保与电子证照

- 新增 PAYMENT、INSURANCE、CERTIFICATE 三类生产网关基础，共 14 个受控操作。
- 新增 `GET /api/financial-gateways` 和 `POST /api/financial-gateways/dispatch`，并复用统一集成事件、死信、人工重试和审计链。
- 支付交易契约 `payment-transaction-v1` 已加入接口字段映射，接口契约达到 9/9 完整映射。
- 金额强制使用整数分；证照正文使用摘要和受控引用；身份证号、手机号、访问令牌、私钥和 Base64 原文禁止进入网关事件。
- 生产环境门禁要求三类 HTTPS 地址及强签名密钥；专项 readiness 明确保持 `productionReady=false`。

### 3. C 端全流程审查

- 居民端新增 10 条业务管线审查台账，覆盖登录、居民首页、健康档案、电子病历、授权审计、消息待办、护理、陪诊、挂号和移动发布。
- 每条管线列出入口、接口、责任方、现场动作、阻断项和证据，支持复制验收清单。
- 审查面板只在 `launch=1` 内部模式展示，正常居民模式保持隐藏。
- 已完成 1440px 桌面和 390px 移动视口验收；移动端审查模式导航改为非遮挡布局，无横向溢出。

## 二、正式生产前已实现的主要标准平台功能

1. 标准治理与评价：六大标准域、自评、校验、整改、证据包和正式割接审批台账。
2. 数据治理与平台底座：数据资产、标准字典、血缘、质量规则、平台总线和二期目录。
3. 接口集成与区域交换：9 类接口契约、签名、幂等、重试、死信、区域共享、转诊和会诊。
4. 身份、安全与审计：多角色会话、机构/居民数据范围、访问日志、安全事件、哈希链和国密适配中心。
5. 医疗质量与运营：质量安全、危急值、病历抽查、整改闭环、床位人力设备监测和药耗监管。
6. 公共卫生与慢病协同：21 个公共卫生标准域、慢病筛查分层、随访、家医、传染病直报和指挥台。
7. 居民连续服务：健康档案、预约挂号、候补、停诊改签、支付退费状态、护理和陪诊。
8. 重点应用：妇幼、免疫、影像云、多点执业、互联网护理、助医陪诊和临床辅助决策。
9. 科研、共享与驾驶舱：科研沙箱、伦理与导出审批、区域共享、指标中心和综合驾驶舱。
10. 运行保障与发布治理：健康检查、SLO、值班、事件、备份恢复模型、发布报告、割接清单和回滚快照。

## 三、验证证据

| 验证项 | 结果 | 说明 |
|---|---:|---|
| `npm.cmd run check` | PASS | 全仓语法检查通过 |
| `npm.cmd test` | 301/301 | API、业务、权限、安全、存储、发布与静态契约全量通过 |
| `npm.cmd run release:report` | 230/230 | 聚合发布门禁全部通过 |
| `npm.cmd run deploy:check` | 189/189 | 报告、网关、环境、运行时与制品检查全部通过 |
| `npm.cmd run launch:smoke -- --base-url=http://127.0.0.1:5241` | 7/7 | 隔离生产预览通过 |
| 浏览器响应式验收 | PASS | 1440x900 与 390x844，10 条管线、无横向溢出、无导航遮挡 |

隔离预览使用 `NODE_ENV=production`、`STORAGE_ENGINE=sqlite`、WAL、FULL 和 5000ms busy timeout。未配置真实外部网关时，状态接口按预期返回未配置且 `productionReady=false`，没有伪造生产通过。

## 四、当前生产阻断项

1. 生产环境、域名、TLS、网络区划、进程守护和密钥托管尚未验收。
2. PostgreSQL 是否作为首期生产库尚待容量与 RTO/RPO 决策；若采用，仍需适配、迁移和原生备份恢复工具。
3. OIDC、短信、医院系统、支付、医保、电子证照和对象存储需要真实凭据、白名单、字典和联合测试回执。
4. 支付/医保回调验签、防重放、乱序处理、日终对账、冲正和差错补偿需按供应商协议落地。
5. 生产监控、告警路由、值班升级、SIEM/WORM 审计保全和首日观察尚未接入目标平台。
6. 等保、密评、渗透、隐私影响评估、漏洞整改、灾备演练和 go/no-go 正式签字尚未完成。

## 五、下一步开发计划

### P0：0-30 天

1. 接入生产可观测性：监控采集、告警路由、值班升级、SIEM/WORM 和接口成功率/延迟/死信指标。
2. 完成密钥与部署自动化：密钥托管、TLS 轮换、不可变制品、环境级流水线、进程编排和回滚自动化。
3. 确定数据库生产路线：完成 SQLite 容量评审；如选 PostgreSQL，交付适配器、迁移校验和备份恢复工具。
4. 获取试点方参数，完成 OIDC、短信、HIS/EMR/LIS/PACS/号源、对象存储、支付、医保和证照沙箱联调。
5. 为支付/医保/证照补齐供应商回调验签、乱序状态机、日终对账与差错处理工作台。

退出条件：生产 `.env` 无占位值；所有 P0 外部接口具备成功、失败、重试和对账回执；监控告警演练通过。

### P1：31-60 天

1. 完成真实主数据映射、历史数据迁移、质量复核和接口字典签字。
2. 执行居民、医生、机构、护理、医保和管理角色 UAT，关闭 P0/P1 缺陷。
3. 完成性能、容量、稳定性、故障注入、备份恢复和安全专项测试。
4. 完成等保密评整改、渗透复测、隐私评估和生产密钥交接。

退出条件：核心业务全链路联合验收，重大安全问题清零，RTO/RPO 达标并归档证据。

### P2：61-90 天

1. 按试点机构和居民白名单灰度开放，持续观察 SLO、错误率、接口对账和居民体验。
2. 执行生产备份恢复、故障切换和回滚演练。
3. 完成首日指挥、事件台、值班交接和多方 go/no-go 审批。
4. 归档生产证据包，生产地址重新执行全量发布门禁和 launch smoke。

退出条件：生产割接清单 10/10 通过，现场签字完整，生产地址 smoke 全部通过。

## 六、下一开发批次建议

下一批优先实施“生产可观测性与 SIEM 告警闭环”，同时建立真实外部网关联合测试参数表。该批次能把现有适配器从“可调用”推进到“可运营、可发现故障、可追责”，也是进入现场灰度前最重要的代码增量。
