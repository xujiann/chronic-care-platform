# 最近两个迭代交付记录

最终候选基线：`baseline/governance-20260803-iteration2-v2`
唯一集成分支：`codex/governance-integration-20260803`

## 迭代一：协作基线与首批领域上下文

- 唯一集成分支和三项历史基线已发布到远端。
- 集成分支已启用必须 PR、Code Owner 审核、过期审核失效、会话解决、线性历史、禁止强推和禁止删除。
- T04、T05 分别提交居民慢病与服务协同依赖清单；T00 负责投影、缺失依赖快速失败和统一路由接入。
- 领域上下文冻结且只暴露显式依赖；额外的全局能力不会泄漏。

## 迭代二：基础链闭环与二级拆分起步

- T01 完成运行与身份安全上下文，T07 完成医保支付上下文，T08 完成外部集成上下文。
- 建立 `identity-authorization`、`financial-external-dispatch`、`signed-appointment-event` 三条版本化共享契约。
- 公共卫生 3 个路由段和临床专科 10 个路由段进入可执行二级子域清单，路由清单可直接展示子域归属。
- 新增跨域组合测试，从运行时投影一直验证到 71 段路由清单，并验证契约缺失时失败关闭。

## 验证结果

- 路由、上下文和进程治理专项测试：15/15 通过。
- 根级测试：263 个文件，1,556 项测试通过，0 失败。
- 部署检查：405/405 通过。
- 演示发布报告：434 项通过，2 项预期告警，总体 `ok=true`。告警为生产发布证据尚未齐备以及 SIEM/Webhook 未配置，未被仓库代码伪造为已完成。

## 后续边界

- T02、T03、T06、T09 仍使用兼容运行时入口，下一轮按同一规范迁移。
- 公共卫生与临床专科当前完成的是可执行子域归属和契约化入口；物理文件拆分应在各自进程分支继续进行。
- 正式生产 Go/No-Go 仍取决于真实环境、外部接口、现场验收与签名证据。
