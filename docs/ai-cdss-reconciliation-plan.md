# AI/CDSS 整合实施计划

基线：2026-09-06 `main@7de328e0`，main CI/Pages 通过；用户已批准整合方向。

选择迁移来源为 PR #248 的 `24b8c01b`，不是整体合并或覆盖；新工作树独立保留原主线内容。

## 范围与顺序

1. 保留 #249 临床中心和 #250 平台中心的页面、查询 schema、来源白名单、采购追踪及测试。
2. 选择迁入 #248 规则状态机、独立审批、来源漂移、回执幂等/CAS、可信范围、共享写锁、审计失败原子性和 state/config 防绕过。
3. 在现有 ai-governance 页面加入规则命令区，不新增页面或另一份规则事实源；医生端保留真实理由和稳定重试键。
4. 临床中心复用统一范围和策略，补跨机构同医生编号、缺可信目录、暂停/漂移双投影隐藏和保留理由统计测试。
5. 同步地图、API 行为证据和精确测试清单，运行标准七门、中央门禁、关键边界覆盖与完整 E2E，完成审阅和替代 PR。

## 非目标与回退

不修改用户原工作树，不整体覆盖主线，不引入模型推理、独立服务、生产迁移或生产激活。回退不得抹除已治理元数据或恢复旧 config 旁路。旧 #248 保留为迁移来源，未经确认不关闭。

## 验收状态

本分支标准七门（build、lint、typecheck、test:unit、test:integration、test:smoke、test:all）均通过；全量发现 517 个测试文件，最终回归 3,031 项通过、0 失败、1 项因未配置真实 PostgreSQL 环境按原有条件跳过。该跳过不代表真实数据库验收通过，仍须远端 PostgreSQL 专项 CI 和现场证据。

routes:check、architecture:test、process:test、platform:iterations:test、repository:governance:verify、process:verify 与 test:coverage:boundaries 均通过。完整浏览器 E2E 73 项全部通过（主应用 57、居民端 13、PWA 3）。旧分支及旧 main 的绿色 CI 不替代本分支远端 CI。

提交前重新 fetch 确认 origin/main 仍为 `7de328e0`。本次仅提交整合分支并创建替代草稿 PR，不合并、不发布、不关闭 #248；生产继续 NO-GO。

独立审阅补充：已关闭回执快照中的无可见提醒规则推荐旁路。返回历史快照前除范围/提醒策略外，还比较全部 rules/ruleConfig 当前公共投影；任一不一致返回稳定 409，不再次写入。专项 70 项及独立复核 33 项通过，审阅未发现本次范围内其他可复现高风险问题。

## 后续队列

影像质控外调与本地提交一致性、体检分流幂等与 CAS、其余临床子域及高风险 API 证据仍为后续独立切片，不计入本次完成。
