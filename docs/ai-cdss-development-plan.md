# AI/CDSS 开发执行计划

基线：2026-09-05 `origin/main@2ac9ab45`；main CI 与 Pages 通过，无开放 PR。用户已批准实施下一步开发计划。

## 首个交付增量

| Owner | 范围 | 验收 |
|---|---|---|
| T00 | Accepted ADR、跨域兼容接线、地图与能力证据、集成审阅 | 契约、路由、所有权、文档门禁通过 |
| T01 | 规则治理领域服务、元数据中心、状态机与命令接口 | 身份拒绝、自审拒绝、摘要绑定、幂等/CAS、持久化失败测试 |
| T06 | 临床读取与回执领域端口、范围隔离、人工处理 | 跨机构/医生拒绝、忽略原因、兼容响应、重复/冲突/错误测试 |
| T00 前端切片 | 治理工作台、访问策略、导航与资产注册 | 加载/空/失败/无权、恶意文本、真实浏览器命令回归 |

采用独立 process 工作树，本地提交后由 T00 集成。原主仓用户变更保持原位。首个增量不包含真实模型推理、新临床事实源或生产激活。

## 验证与交付

先运行领域测试，再运行 build、lint、typecheck、unit、integration、smoke 和 test:all；中央变更额外运行 routes:check、architecture:test、process:test、platform:iterations:test、repository:governance:verify。触及身份和审计时补关键边界覆盖，前端运行专项 Chromium E2E。集成后审阅高风险路径，失败返回修复，不放宽测试。通过后形成可审阅提交与测试记录。

## 后续批次

1. 五个临床子域依次补影像外调一致性、体检幂等/CAS、质量安全闭环、血液与急救边界。
2. 高风险写接口按真实风险分批补端点级行为证据。
3. 待确认数据集合按 Owner 分批审查，并补可执行迁移核对与恢复。
4. 高风险页面继续迁移可信渲染；触达的宽上下文小步收窄。
5. 完整发布候选回归；外部系统接入和现场验收保持独立待办，不以仓库测试替代。

上述后续批次未因本首个增量自动标记完成，实际完成状态以提交与测试证据为准。

## 本轮验收记录（2026-09-05）

- build、lint、typecheck、test:unit、test:integration、test:smoke、test:all 全部通过。单元入口覆盖 446 个文件，集成入口覆盖 66 个文件；全量自动发现覆盖 512 个文件、13 批，没有删除或跳过失败测试。
- 完整 Chromium E2E 通过：主平台 55 项、居民端 13 项、PWA 3 项，共 71 项。
- routes:check、architecture:test（117 项）、process:test、platform:iterations:test、repository:governance:verify、process:verify 全部通过；所有权检查无违规。
- 验收中修正了新增路由、静态页面、资产与测试分区的精确清单，并将新真实 HTTP 集成测试同步至旧 npm test 入口；原风险阈值保持不变。
- 集成审阅覆盖来源漂移、自审拒绝、跨机构/医生访问、治理写入绕过、共享锁、审计和存储失败、浏览器实际理由与稳定重试。测试日志保存在本工作树的忽略目录 `tmp/ai-cdss-validation/final-*.log`，不是生产证明。
- 本增量进入可提交审阅状态，未合并、未发布、未激活生产功能。后续批次仍未完成。

## 下一切片的已确认边界

PR #248 首轮 CI 的后端、全量和治理检查通过，浏览器首个治理导航用例因导航计数为 0 超时失败。测试现改为登录前安装该用例拦截，并明确等待鉴权与导航就绪，保留导航数量、恶意文本、移动端及权限断言和原超时。调整后本地该用例连续 5 次、完整 E2E 71 项、lint、typecheck、所有权及 5 项 UI/浏览器分区测试通过；远端 CI 需对新提交重新验证，不能沿用旧提交的通过状态。

下一步优先治理影像质控：现有 `study-quality-control-command` 的外部 FHIR 调用先于本地提交，需明确失败后核对与恢复、幂等、版本和可信机构范围；不能仅增加进程锁后宣称一致性问题解决。随后治理体检专项分流的幂等、版本与审计单次提交。两项均应使用独立 process 切片、补具体 ADR 和迁移/回滚合同，保留原用例特征测试；本轮没有改动这些运行时路径。
